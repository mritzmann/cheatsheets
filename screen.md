# Screen

### New session:

```shell
screen -S <session-name>
```

### Detach session:

<kbd>Ctrl+A</kbd> then press <kbd>d</kbd>

### List sessions:

```shell
screen -ls
```

### Re-attach session:

```shell
screen -r <session-name>
```
